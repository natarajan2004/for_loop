#include<iostream>
using namespace std;
int main()
{
    string str;
    
    
    int n,i,j;
    
    cout<<"Enter the strings:";
    
        getline(cin, str);
        
            if(str==" a" || str==" e" || str==" i" || str==" i" || str==" u" || str==" A" || str==" E" || str==" I" || str==" O" || str== " U")
            {
                cout<<str;
            }
        
    }
